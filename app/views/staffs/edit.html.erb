<div class="container py-5">
  <div class="row justify-content-center">
    <div class="col-md-6">
      <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
          <h2 class="mb-0">Edit Staff</h2>
        </div>
        <div class="card-body">
          <%= form_with model: @staff, url: staff_path(@staff), method: :patch, local: true do |f| %>
            <div class="mb-3">
              <%= f.label :name, class: "form-label" %>
              <%= f.text_field :name, class: "form-control", placeholder: "Staff Name", required: true %>
            </div>

            <div clasindexs="mb-3">
              <%= f.label :email, class: "form-label" %>
              <%= f.email_field :email, class: "form-control", placeholder: "Staff Email", required: true %>
            </div>

            <div class="mb-3">
              <%= f.label :password, class: "form-label" %>
              <%= f.password_field :password, class: "form-control", placeholder: "Password (Leave blank to keep current)", required: false %>
            </div>

            <div class="mb-3">
              <%= f.label :password_confirmation, class: "form-label" %>
              <%= f.password_field :password_confirmation, class: "form-control", placeholder: "Confirm Password", required: false %>
            </div>

            <!-- Staff Role Field -->
            <div class="mb-3">
              <%= f.label :staff_role, class: "form-label" %>
              <%= f.select :staff_role, User::ROLES.map { |role| [role.humanize, role] }, selected: @staff.staff_role, class: "form-control", required: true %>
            </div>

            <%= f.submit "Update Staff", class: "btn btn-primary" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
